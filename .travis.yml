language: generic
dist: trusty
addons:
  apt:
    packages:
      - texlive-lang-cjk
      - texlive-latex-recommended
      - texlive-fonts-recommended
script:
  - ptex2pdf -l pasj.tex
  - mkdir public
  - mv pasj.pdf public
deploy:
  provider: pages
  local-dir: public
  target-branch: gh-pages
  github-token: $GITHUB_TOKEN
  skip-cleanup: true
  keep-history: true
  on:
    branch: master
